#!/usr/bin/env bash

function bootstrap {
  local shome="$(unset CDPATH; cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"

  git clone git@github.com:defn/pkgsrc "$shome/vendor/pkgsrc"
}

bootstrap
